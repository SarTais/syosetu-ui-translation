# Fixtures

This directory is for local reference files used while developing translations.

Saved Syosetu/Narou HTML pages should go in:

```text
fixtures/pages/
```

Fixture files are not bundled into the userscript.
